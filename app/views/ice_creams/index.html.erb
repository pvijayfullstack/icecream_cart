<h1>Listing ice_creams</h1>

<table>
  <tr>
    <th>Cone for cup</th>
    <th>Flavor</th>
    <th>Extras</th>
    <th>Total number</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ice_creams.each do |ice_cream| %>
  <tr>
    <td><%= ice_cream.cone_for_cup_id %></td>
    <td><%= ice_cream.flavor_id %></td>
    <td><%= ice_cream.extras_id %></td>
    <td><%= ice_cream.total_number %></td>
    <td><%= ice_cream.user_id %></td>
    <td><%= link_to 'Show', ice_cream %></td>
    <td><%= link_to 'Edit', edit_ice_cream_path(ice_cream) %></td>
    <td><%= link_to 'Destroy', ice_cream, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ice cream', new_ice_cream_path %>
